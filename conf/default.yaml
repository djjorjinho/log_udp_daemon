---
daemon:
  name: log_udp_daemon
  pid: /tmp/log_udp_daemon.pid
server:
  port: 9011
logging:
  driver: MongoDB
  mandatory_fields:
    - class
    - method
    - hostname
    - timestamp
MongoDB:
  host: localhost
  port: 9002
  db: log_daemon
